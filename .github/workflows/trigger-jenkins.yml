name: Trigger Jenkins Workflow
on:
  workflow_call:

jobs:
  trigger:
    name: Trigger jenkins workflow
    runs-on: ubuntu-latest
    steps:
      - name: Trigger
        uses: appleboy/jenkins-action@master
        with:
          url: "https://1524-2a09-bac5-d46e-16dc-00-247-1b.ngrok-free.app"
          user: "20424010"
          token: ${{ secrets.JENKINS_TOKEN }}
          job: "ACN-Project-3-Workflow"
